# HR Tech Frontend — documentation

All project docs live here so nothing is scattered under `src/` or the repo root (except a short [root README](../README.md) for GitHub).

| Document | Description |
|----------|-------------|
| [Getting started](./getting-started.md) | Stack, install, run, Git workflow, commits, imports, project tree |
| [Architecture](./architecture.md) | Layers (`core` / `features` / `shared`), dependency rules, where to put code |
| [Roles](./roles.md) | Role model, `roles.constants`, routes/menu, how to add or change roles |

---

**Quick start:** `npm install` → `npm start` → http://localhost:4200  

**Lint:** `npm run lint` (ESLint + import path checks).
